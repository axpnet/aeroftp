services:
  rsync-sshd:
    build:
      context: .
      dockerfile: Dockerfile.native-sshd
    container_name: aeroftp-rsync-native
    ports:
      - "2223:22"
    volumes:
      - ./keys:/keys:ro
      - ./workspace:/workspace
      - ./bin/aerorsync_serve:/opt/aerorsync/bin/aerorsync_serve:ro
    restart: unless-stopped